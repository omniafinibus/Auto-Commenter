Todos for Auto-Commenter:
	# = Info
	[ ] = To do
	[!] = Important
	[x] = Completed
	[o] = In progress
	[-] = Failed

[!] Clean up folder structure
[!] Remove hardcoded paths
[ ] Implement a per function/class commenter (requires parser)
    [ ] Python code
    [ ] C/C++
[ ] Fine tune prompt template
[ ] Implement parallelism for comment generation and user input
[ ] Implement text comparison